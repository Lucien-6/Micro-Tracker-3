# IDE / tooling
.cursor/
.codegraph/

# Session data
.history

# Model weights (local only — never commit checkpoint files)
model_weights/**
!model_weights/.gitkeep

# Common weight extensions anywhere under model_weights (redundant safety)
model_weights/**/*.pt
model_weights/**/*.pth
model_weights/**/*.ckpt
model_weights/**/*.safetensors
model_weights/**/*.bin

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Exported tracking results (optional local output)
*_MT-Results_*/
